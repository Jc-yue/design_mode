一、代理模式定义：给目标对象提供一个代理对象，并由代理对象控制目标对象的引用

二、静态代理的特点
    1、在编译期就就已经将接口、被代理类（目标类）、代理类编译成了class文件
    2、静态代理在使用时，需要定义一个接口或者父类，目标对象（被代理对象）与代理对象共同实现接口或者继承父类

三、静态代理总结
    优点：1、在一定程度上降低了系统的耦合度
         2、可以做到不对目标对象进行修改的前提下，对目标对象进行功能的扩展和拦截

    缺点：由于代理类和被代理类（目标类）都实现了共有的接口，如果接口方法有增加或者修改，那么代理类和被代理类都需要修改，
         如果接口的实现类和代理类比较多，那么不但没有降低系统耦合度，反而还增加了系统耦合度